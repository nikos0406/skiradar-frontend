services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: skiradar-frontend:latest
    ports:
      - "3030:3000"
    environment:
      - NODE_ENV=production
    env_file:
      - .env.local
